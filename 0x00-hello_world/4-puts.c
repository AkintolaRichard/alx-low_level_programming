#include <stdio.h>

/**
 * main - Outputs strings using puts function
 *
 * Return: Always 0
 */
int main(void)
{
	char myMsg[] = "\"Programming is like building a multiligual puzzle";

	puts(myMsg);

	return (0);
}
